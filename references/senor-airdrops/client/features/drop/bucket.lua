--[[ 
    Note from script developer: this feature was 
    intended to use on my own server, you can either ignore this file
    or implement it by yourself, its pretty much irrelveant.
]]

-- local CoreDrops = require 'client.core.drops'

-- AddStateBagChangeHandler('bucket', ('player:%s'):format(cache.serverId), function(_, _, value)
--     if value ~= 0 then
--         CoreDrops.removeDrops()
--         return
--     end

--     CoreDrops.CreateDrops()
-- end)
