Config = Config or {}

Config.Compatibility = {
    enabled = true,       -- if you wanna support old chat exports / events.
    consolePrints = true, -- if disabled you won't see prints from client/server on F8 / server console
}

Config.General = {
    blockNonCommandMessages = false,
    adminBypassLockdown = false,
}
