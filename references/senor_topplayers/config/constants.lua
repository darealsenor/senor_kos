ALLOWED_STAT_FIELDS = {
    kills = true,
    deaths = true,
    damage = true,
    headshots = true,
    playtime = true,
    money = true,
    vehicles = true,
    properties = true,
}

DEFAULT_STAT_CATEGORY = 'kills'

PLACEMENT_PED_MODELS = {
    'mp_m_freemode_01',
}
